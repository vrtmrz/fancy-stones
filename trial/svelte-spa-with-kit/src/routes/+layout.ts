// export const prerender = true;

export const prerender = true;
// To make the app completely SPA, we can set the following export to false. However, mostly no one will need it.
// export const ssr = false;